Step 1. Install Virtual Enviroment
-python -m venv myvenv(ENv NAme)

Step 2-.\myvenv\Scripts\activate 

Step 3. Install Django
-pip install django

Step 4. Start Project
-django-admin startproject mysite .(Site Name)

step 5. Start App
-django-admin startapp myapp  (App Name)

Step 6. Migrate
- python .\manage.py migrate

Step 7. Create Super User
- python .\manage.py superuser

Step 8. Register App
->mysite->setting.py-> in installedapp write('myapp'),
->set tz->Asia/Kolkata

Step 8. Urls
->mysite->urls.py->
from django.urls import path,include(18 line)
urlpatterns = [
    path('admin/', admin.site.urls),
    path('', include('myapp.urls')),
]

Step 9. Copy Urls To my app
a.craete urls.py in my app
paste-
from django.urls import path
from .import views

urlpatterns = [
    path('', views.index,name='index'),
]

Step 10. Views
Create FUnction-
def index(request):
    return render(request,'index.html')

Step 11. create 2 folder 
1.templates
2.static in my app

Step 12. Copy
Copy folders->static
     html files->templates

Step 13.index on top add ->{% loadstatic %}

Step 14.add ({% static ')css/bootstrap.min.css(' %}) ,() accordingly

now setupcomplete